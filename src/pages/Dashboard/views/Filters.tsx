import type React from "react";
import type { StepName } from "..";

export default function Filters({ }: { setStep: React.Dispatch<React.SetStateAction<StepName>> }) {
  
  return (
    <div>Filters</div>
  )
}
